[providers]
python = true

[phases.setup]
nixPkgs = ['python311', 'pip']

[phases.install]
cmds = ['cd python_backend && pip install -r requirements.txt']

[phases.build]
cmds = []

[start]
cmd = 'gunicorn python_backend.app:app --bind 0.0.0.0:$PORT --workers 2 --timeout 120 --chdir python_backend'

